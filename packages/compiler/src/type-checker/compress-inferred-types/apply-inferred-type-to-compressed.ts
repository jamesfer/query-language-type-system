import { InferredType } from '../converge-values/converge-types';
import { CompressedInferredTypes } from './merge-compressed-inferred-types';

export function applyInferredTypeToCompressed(inferredType: InferredType, compressedInferredTypes: CompressedInferredTypes): CompressedInferredTypes {

}
