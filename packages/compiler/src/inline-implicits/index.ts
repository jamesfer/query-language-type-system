import { ResolvedImplicitNode } from '../type-checker/resolve-implicits/resolved-implicit-node';

/**
 * Transforms resolved implicit annotations on nodes into parameters.
 */
export function inlineImplicits(node: ResolvedImplicitNode): ResolvedImplicitNode {
}
